
  # CiP Member Portal

  This is a code bundle for CiP Member Portal. The original project is available at https://www.figma.com/design/DELoPSsDaGFowJepaIfOpf/CiP-Member-Portal.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  